# UI Тесты для Max Bot Manager

Эта папка содержит рабочие UI тесты для проверки веб-интерфейса Max Bot Manager через браузер.

## 📁 Структура

```
tests/ui/
├── README.md                    # Эта инструкция
├── MCP_QUICKSTART.md            # Быстрый старт MCP Selenium для Kilo Code
├── MCP_SELENIUM_SETUP.md        # Полная инструкция по MCP Selenium
├── AI_INSTRUCTION.md            # Инструкция для ИИ по UI тестированию
├── mcp_config.example.json      # Пример конфигурации MCP
├── selenium_test.py             # UI тесты через Selenium (Python)
├── playwright_test.js           # UI тесты через Playwright (Node.js)
└── package.json                 # Зависимости Node.js для Playwright
```

## 🤖 MCP Selenium - Автоматическое UI тестирование ИИ

**Новая возможность!** Теперь вы можете использовать MCP Selenium для автоматического UI тестирования через ИИ (Kilo Code).

### Что это даёт?

- ✅ ИИ сам пишет и выполняет тесты по вашему описанию
- ✅ Естественный язык для описания задач
- ✅ Автоматическая адаптация к изменениям UI
- ✅ Умная диагностика проблем
- ✅ Поддержка localhost сайтов

### Быстрый старт

```bash
# 1. Установите зависимости
pkg install nodejs chromium

# 2. Установите MCP Selenium
npm install -g @angiejones/mcp-selenium

# 3. Настройте конфигурацию (см. MCP_QUICKSTART.md)
```

### Примеры задач для ИИ

```
"Протестируй кнопку на главной странице, не нажимается"
"Проверь форму создания бота, заполни её тестовыми данными"
"Найди все визуальные баги на странице редактора flow"
"Сделай скриншот страницы после нажатия кнопки"
```

### Документация

- 📖 [Быстрый старт MCP Selenium](./MCP_QUICKSTART.md) - начните здесь
- 📚 [Полная инструкция MCP Selenium](./MCP_SELENIUM_SETUP.md) - детальное руководство
- 🤖 [Инструкция для ИИ](./AI_INSTRUCTION.md) - как ИИ должен писать тесты

## 🚀 Быстрый запуск

### Через Playwright (рекомендуется)

```bash
# Из корня проекта
cd tests/ui
npm install
npm test
```

### Через Selenium

```bash
# Из корня проекта
python tests/ui/selenium_test.py
```

## 📋 Подготовка окружения

### Для Playwright (Node.js)

1. **Установить Node.js** (обычно уже установлен в Termux):
   ```bash
   node --version  # должно быть v18+
   ```

2. **Установить Chromium** (если не установлен):
   ```bash
   pkg install x11-repo
   pkg install chromium
   ```

3. **Установить зависимости**:
   ```bash
   cd tests/ui
   npm install
   ```

### Для Selenium (Python)

1. **Установить Python** (обычно уже установлен):
   ```bash
   python --version  # должно быть 3.8+
   ```

2. **Установить Chromium** (если не установлен):
   ```bash
   pkg install x11-repo
   pkg install chromium
   ```

3. **Установить Selenium**:
   ```bash
   pip install selenium
   ```

## 🔧 Обязательные параметры

### Playwright

```bash
# Обязательные переменные окружения
export CHROMIUM_PATH=/data/data/com.termux/files/usr/bin/chromium-browser
export PLAYWRIGHT_BROWSERS_PATH=0
export APPLICATION_ROOT=/manage  # для вашего проекта
```

**Почему эти параметры обязательны:**

- `CHROMIUM_PATH` - путь к системному Chromium в Termux
- `PLAYWRIGHT_BROWSERS_PATH=0` - отключает автоматическую загрузку браузеров Playwright
- `APPLICATION_ROOT` - префикс URL для вашего Flask приложения

**Аргументы запуска Chromium:**
```javascript
{
  executablePath: process.env.CHROMIUM_PATH,
  headless: true,  // или false для графического режима
  args: [
    '--no-sandbox',        // обязательно для Termux
    '--disable-gpu',       // обязательно для Termux
    '--disable-dev-shm-usage',
    '--disable-dev-tools'
  ]
}
```

### Selenium

```python
# Обязательные аргументы для Chrome в Termux
from selenium.webdriver.chrome.options import Options as ChromeOptions

options = ChromeOptions()
options.add_argument('--headless=new')        # headless режим
options.add_argument('--no-sandbox')          # обязательно для Termux
options.add_argument('--disable-dev-shm-usage')  # обязательно для Termux
options.add_argument('--disable-gpu')
options.add_argument('--disable-dev-tools')
options.add_argument('--no-zygote')
options.add_argument('--single-process')
options.add_argument('--window-size=1920,1080')

# Обязательно указать путь к chromedriver
from selenium.webdriver.chrome.service import Service
service = Service(executable_path='/data/data/com.termux/files/usr/bin/chromedriver')

driver = webdriver.Chrome(service=service, options=options)
```

## 📝 Что проверяют тесты

### Общие тесты (оба инструмента)

1. **Layout главной страницы**
   - Наличие заголовка H1
   - Количество кнопок и форм
   - Отображение элементов интерфейса

2. **Форма создания бота**
   - Наличие кнопки "Создать бота"
   - Кликабельность кнопок
   - Наличие полей ввода

3. **Редактор Flow**
   - Наличие контейнера редактора
   - Кнопка сохранения
   - Количество кнопок на странице

4. **Responsive Design**
   - Desktop (1920x1080)
   - Laptop (1366x768)
   - Tablet (768x1024)
   - Mobile (375x667)

5. **Ошибки в консоли браузера**
   - JavaScript ошибки
   - 404 ошибки (например, favicon.ico)

## 🆚 Selenium vs Playwright

| Характеристика | Selenium | Playwright |
|----------------|----------|------------|
| Язык | Python | JavaScript/Node.js |
| Скорость | Медленнее | Быстрее |
| Стабильность | Менее стабильный | Более стабильный |
| API | Более сложный | Более простой |
| Установка | Проще | Сложнее (нужен Node.js) |
| **Рекомендация** | Для простых тестов | **Для полноценного тестирования** |

## 🐛 Поиск багов

Тесты автоматически ищут следующие баги:

- Отсутствующие элементы (H1, кнопки, формы)
- Некликабельные кнопки
- Сломанный layout на разных размерах экрана
- Ошибки JavaScript в консоли
- Отсутствие favicon.ico (404 ошибка)

## 📊 Результаты тестов

После запуска вы увидите:

```
============================================================
ФИНАЛЬНЫЕ РЕЗУЛЬТАТЫ
============================================================
Всего тестов: 5
Пройдено: 5
Не пройдено: 0

Найдено багов: 0
```

Если баги найдены, они будут выведены с описанием и уровнем серьёзности:
- **LOW** - минорные проблемы
- **MEDIUM** - средняя серьёзность
- **HIGH** - серьёзные проблемы
- **CRITICAL** - критические ошибки

## 🔧 Решение проблем

### Проблема: "chromedriver not found"

**Решение:**
```bash
# Проверить наличие chromedriver
ls -la /data/data/com.termux/files/usr/bin/ | grep chrom

# Если нет, переустановить chromium
pkg reinstall chromium
```

### Проблема: "EACCES: permission denied" при npm install

**Решение:**
```bash
npm install --no-bin-links
```

### Проблема: "Failed to connect to localhost:5000"

**Решение:**
```bash
# Убедитесь, что Flask приложение запущено
export APPLICATION_ROOT=/manage
python src/app.py &
```

### Проблема: "Playwright not found"

**Решение:**
```bash
# Использовать playwright-core вместо playwright
npm install playwright-core
```

## 📚 Дополнительные ресурсы

- [Playwright Documentation](https://playwright.dev/)
- [Selenium Documentation](https://www.selenium.dev/documentation/)
- [playwright-termux](https://github.com/Jobians/playwright-termux) - инструкция по запуску Playwright в Termux

## 🎯 Советы

1. **Используйте Playwright** для более надёжных и быстрых тестов
2. **Запускайте тесты регулярно** при изменении кода
3. **Проверяйте разные размеры экранов** для responsive design
4. **Смотрите на ошибки в консоли** - они могут указывать на скрытые проблемы
5. **Сохраняйте скриншоты** при ошибках для отладки

## 📝 Примечания

- Тесты автоматически запускают и останавливают Flask приложение
- Все тесты запускаются в headless режиме (без графического интерфейса)
- Для Termux обязательно использовать системный Chromium
- Playwright работает быстрее и стабильнее Selenium
