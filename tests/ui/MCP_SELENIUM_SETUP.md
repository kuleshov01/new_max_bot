# 🚀 MCP Selenium: Установка и настройка для Termux

Эта инструкция поможет вам настроить MCP Selenium Server для автоматического UI тестирования через Kilo Code.

## 📋 Что такое MCP Selenium?

**MCP Selenium** - это Model Context Protocol (MCP) сервер, который предоставляет инструменты Selenium WebDriver через стандартный протокол MCP. Это позволяет ИИ-агентам (включая Kilo Code) автоматически управлять браузером и выполнять UI тесты.

### Возможности MCP Selenium:

- ✅ Запуск браузерных сессий (Chrome, Firefox, Edge)
- ✅ Навигация по URL
- ✅ Поиск элементов (ID, CSS, XPath, Name, Tag, Class)
- ✅ Клик по элементам
- ✅ Ввод текста в поля
- ✅ Получение текста элементов
- ✅ Действия мышью (hover, drag & drop, double click, right click)
- ✅ Нажатие клавиш клавиатуры
- ✅ Скриншоты
- ✅ Загрузка файлов
- ✅ Headless режим

## 🔧 Требования

### Обязательные компоненты:

1. **Node.js** (v18+) - для запуска MCP сервера
2. **npm** - менеджер пакетов Node.js
3. **Chromium** - браузер для тестирования
4. **ChromeDriver** - драйвер для управления Chromium

### Проверка требований в Termux:

```bash
# Проверяем Node.js
node --version
# Должно быть v18+

# Если не установлен, устанавливаем
pkg install nodejs

# Проверяем npm
npm --version

# Проверяем Chromium
which chromium-browser
# Или
ls -la /data/data/com.termux/files/usr/bin/chromium-browser

# Если не установлен, устанавливаем
pkg install x11-repo
pkg install chromium

# Проверяем ChromeDriver
which chromedriver
# Или
ls -la /data/data/com.termux/files/usr/bin/chromedriver

# ChromeDriver обычно устанавливается вместе с chromium
# Если нет, переустановите chromium
pkg reinstall chromium
```

## 📦 Установка MCP Selenium

### Способ 1: Глобальная установка (рекомендуется)

```bash
# Устанавливаем глобально
npm install -g @angiejones/mcp-selenium

# Проверяем установку
which mcp-selenium
# Или
npm list -g @angiejones/mcp-selenium
```

### Способ 2: Использование без установки (через npx)

```bash
# MCP Selenium будет запускаться через npx
# Ничего устанавливать не нужно, если есть Node.js и npm
```

## ⚙️ Настройка MCP сервера

### Файл конфигурации MCP

MCP клиенты (включая Kilo Code) используют конфигурационный файл для подключения к MCP серверам. Обычно это файл `mcp_config.json` или аналогичный.

### Создание конфигурации для Kilo Code:

Создайте файл конфигурации MCP сервера:

```json
{
  "mcpServers": {
    "selenium": {
      "command": "npx",
      "args": [
        "-y",
        "@angiejones/mcp-selenium"
      ],
      "env": {
        "CHROME_PATH": "/data/data/com.termux/files/usr/bin/chromium-browser",
        "CHROMEDRIVER_PATH": "/data/data/com.termux/files/usr/bin/chromedriver"
      }
    }
  }
}
```

### Альтернативный вариант с глобальной установкой:

```json
{
  "mcpServers": {
    "selenium": {
      "command": "mcp-selenium",
      "args": [],
      "env": {
        "CHROME_PATH": "/data/data/com.termux/files/usr/bin/chromium-browser",
        "CHROMEDRIVER_PATH": "/data/data/com.termux/files/usr/bin/chromedriver"
      }
    }
  }
}
```

## 🔌 Подключение к Kilo Code

Kilo Code поддерживает MCP серверы через стандартный протокол. После настройки конфигурации:

1. **Перезапустите Kilo Code**, чтобы он загрузил новую конфигурацию MCP сервера

2. **Проверьте доступные MCP инструменты** - Kilo Code автоматически обнаружит MCP Selenium и добавит его инструменты

3. **Используйте инструменты MCP Selenium** напрямую из Kilo Code

## 🎯 Использование MCP Selenium с Kilo Code

После успешного подключения, Kilo Code будет иметь доступ к следующим инструментам:

### 1. start_browser - Запуск браузера

```javascript
{
  "tool": "start_browser",
  "parameters": {
    "browser": "chrome",
    "options": {
      "headless": true,
      "arguments": [
        "--no-sandbox",
        "--disable-gpu",
        "--disable-dev-shm-usage"
      ]
    }
  }
}
```

**Для Termux обязательно используйте эти аргументы:**
- `--no-sandbox` - обязательно для Termux
- `--disable-gpu` - обязательно для Termux
- `--disable-dev-shm-usage` - обязательно для Termux

### 2. navigate - Переход на URL

```javascript
{
  "tool": "navigate",
  "parameters": {
    "url": "http://localhost:5000/manage"
  }
}
```

### 3. find_element - Поиск элемента

```javascript
{
  "tool": "find_element",
  "parameters": {
    "by": "css",
    "value": ".btn-primary",
    "timeout": 10000
  }
}
```

**Поддерживаемые локаторы:**
- `id` - поиск по ID
- `css` - поиск по CSS селектору
- `xpath` - поиск по XPath
- `name` - поиск по атрибуту name
- `tag` - поиск по тегу
- `class` - поиск по классу

### 4. click_element - Клик по элементу

```javascript
{
  "tool": "click_element",
  "parameters": {
    "by": "css",
    "value": "button[type='submit']",
    "timeout": 10000
  }
}
```

### 5. send_keys - Ввод текста

```javascript
{
  "tool": "send_keys",
  "parameters": {
    "by": "name",
    "value": "username",
    "text": "testuser",
    "timeout": 10000
  }
}
```

### 6. get_element_text - Получение текста элемента

```javascript
{
  "tool": "get_element_text",
  "parameters": {
    "by": "css",
    "value": ".message",
    "timeout": 10000
  }
}
```

### 7. take_screenshot - Скриншот

```javascript
{
  "tool": "take_screenshot",
  "parameters": {
    "outputPath": "/sdcard/Project/new_max_bot/screenshot.png"
  }
}
```

### 8. close_session - Закрытие браузера

```javascript
{
  "tool": "close_session",
  "parameters": {}
}
```

## 🌐 Тестирование Localhost сайтов

**Да, MCP Selenium может тестировать сайты на localhost!**

### Пример сценария:

```javascript
// 1. Запускаем браузер
{
  "tool": "start_browser",
  "parameters": {
    "browser": "chrome",
    "options": {
      "headless": true,
      "arguments": ["--no-sandbox", "--disable-gpu", "--disable-dev-shm-usage"]
    }
  }
}

// 2. Переходим на localhost
{
  "tool": "navigate",
  "parameters": {
    "url": "http://localhost:5000/manage"
  }
}

// 3. Ищем кнопку
{
  "tool": "find_element",
  "parameters": {
    "by": "css",
    "value": "button",
    "timeout": 10000
  }
}

// 4. Кликаем по кнопке
{
  "tool": "click_element",
  "parameters": {
    "by": "css",
    "value": "button",
    "timeout": 10000
  }
}

// 5. Делаем скриншот
{
  "tool": "take_screenshot",
  "parameters": {
    "outputPath": "/sdcard/Project/new_max_bot/test_screenshot.png"
  }
}

// 6. Закрываем браузер
{
  "tool": "close_session",
  "parameters": {}
}
```

## 📝 Примеры задач для ИИ

После настройки MCP Selenium, вы можете давать ИИ такие задачи:

### Пример 1: Проверка кнопки

**Задача:** "Протестируй кнопку создания бота на главной странице http://localhost:5000/manage, проверь что она нажимается"

**Что сделает ИИ:**
1. Запустит браузер через MCP Selenium
2. Перейдёт на указанный URL
3. Найдёт кнопку
4. Проверит её видимость и кликабельность
5. Кликнет по кнопке
6. Сделает скриншот
7. Закроет браузер
8. Предоставит отчёт

### Пример 2: Проверка формы

**Задача:** "Проверь форму создания бота на http://localhost:5000/manage, заполни её тестовыми данными"

**Что сделает ИИ:**
1. Запустит браузер
2. Перейдёт на страницу
3. Найдёт поля формы
4. Заполнит их тестовыми данными
5. Отправит форму
6. Проверит результат
7. Предоставит отчёт

### Пример 3: Поиск багов

**Задача:** "Найди все визуальные баги на странице http://localhost:5000/manage/flow-editor"

**Что сделает ИИ:**
1. Запустит браузер
2. Перейдёт на страницу
3. Проверит наличие всех элементов
4. Проверит консоль на ошибки JavaScript
5. Сделает скриншоты
6. Проверит responsive design
7. Предоставит список найденных багов

## 🐛 Устранение неполадок

### Проблема: "Cannot find module '@angiejones/mcp-selenium'"

**Решение:**
```bash
# Установите пакет глобально
npm install -g @angiejones/mcp-selenium

# Или используйте npx без установки
npx -y @angiejones/mcp-selenium
```

### Проблема: "ChromeDriver not found"

**Решение:**
```bash
# Переустановите chromium
pkg reinstall chromium

# Проверьте наличие ChromeDriver
ls -la /data/data/com.termux/files/usr/bin/chromedriver

# Укажите путь в конфигурации MCP
"env": {
  "CHROMEDRIVER_PATH": "/data/data/com.termux/files/usr/bin/chromedriver"
}
```

### Проблема: "Failed to connect to Chrome"

**Решение:**
```bash
# Убедитесь, что используете правильные аргументы для Termux
"arguments": [
  "--no-sandbox",
  "--disable-gpu",
  "--disable-dev-shm-usage",
  "--disable-dev-tools"
]
```

### Проблема: "Cannot connect to localhost"

**Решение:**
```bash
# Убедитесь, что Flask приложение запущено
export APPLICATION_ROOT=/manage
python src/app.py &

# Проверьте, что порт доступен
curl http://localhost:5000/manage
```

### Проблема: "MCP server not found"

**Решение:**
1. Проверьте конфигурацию MCP
2. Перезапустите Kilo Code
3. Проверьте логи Kilo Code на предмет ошибок MCP

## 📚 Полный пример конфигурации

### mcp_config.json

```json
{
  "mcpServers": {
    "selenium": {
      "command": "npx",
      "args": [
        "-y",
        "@angiejones/mcp-selenium"
      ],
      "env": {
        "CHROME_PATH": "/data/data/com.termux/files/usr/bin/chromium-browser",
        "CHROMEDRIVER_PATH": "/data/data/com.termux/files/usr/bin/chromedriver",
        "SELENIUM_HEADLESS": "true"
      }
    }
  }
}
```

## 🎯 Преимущества MCP Selenium

### По сравнению с обычным Selenium:

| Характеристика | Обычный Selenium | MCP Selenium |
|---------------|-----------------|--------------|
| **Интеграция с ИИ** | Требует написания кода | Прямое использование ИИ |
| **Настройка** | Сложная | Простая (одна команда) |
| **Гибкость** | Жёсткие тесты | Адаптивные проверки |
| **Отчёты** | Требуют кода | Автоматические |
| **Скорость разработки** | Медленная | Быстрая |

### Для вашего проекта:

✅ **ИИ сможет сам писать тесты** - просто опишите задачу словами
✅ **Автоматическая адаптация** - ИИ подстроится под изменения UI
✅ **Умная диагностика** - ИИ найдёт причины проблем
✅ **Естественные отчёты** - понятные описания багов
✅ **Быстрое начало** - минимум настроек

## 📖 Дополнительные ресурсы

- [MCP Selenium GitHub](https://github.com/angiejones/mcp-selenium)
- [MCP Protocol Documentation](https://modelcontextprotocol.io/)
- [Selenium Documentation](https://www.selenium.dev/documentation/)

## 💡 Советы

1. **Всегда используйте headless режим** в Termux для экономии ресурсов
2. **Добавляйте скриншоты** при ошибках для отладки
3. **Используйте ожидания (timeout)** для динамических страниц
4. **Тестируйте на разных размерах экрана** для responsive design
5. **Проверяйте консоль браузера** на ошибки JavaScript
6. **Начинайте с простых тестов** и усложняйте постепенно

## 🚀 Быстрый старт

```bash
# 1. Установите зависимости
pkg install nodejs chromium

# 2. Установите MCP Selenium
npm install -g @angiejones/mcp-selenium

# 3. Создайте конфигурацию MCP
# (см. раздел "Настройка MCP сервера")

# 4. Перезапустите Kilo Code

# 5. Дайте ИИ задачу:
# "Протестируй главную страницу http://localhost:5000/manage"
```

---

**Готово!** Теперь ИИ может автоматически тестировать ваш UI через MCP Selenium! 🎉
