# 🚀 MCP Selenium - Быстрый старт для Kilo Code

## 📋 Что это даёт?

После настройки MCP Selenium, вы сможете давать ИИ задачи на естественном языке:

**Примеры задач:**
- "Протестируй кнопку на главной странице, не нажимается"
- "Проверь форму создания бота, заполни её тестовыми данными"
- "Найди все визуальные баги на странице редактора flow"
- "Сделай скриншот страницы после нажатия кнопки"

ИИ **сам** запустит браузер, перейдёт на страницу, найдёт элементы, выполнит действия и предоставит отчёт!

## ⚡ Быстрая установка (3 шага)

### Шаг 1: Установка зависимостей

```bash
# Устанавливаем Node.js и Chromium
pkg install nodejs chromium

# Проверяем установку
node --version  # должно быть v18+
chromium-browser --version
```

### Шаг 2: Установка MCP Selenium

```bash
# Глобальная установка (рекомендуется)
npm install -g @angiejones/mcp-selenium

# Проверяем установку
npm list -g @angiejones/mcp-selenium
```

### Шаг 3: Настройка конфигурации MCP

Скопируйте файл конфигурации в нужное место:

```bash
# Пример конфигурации уже создан:
cp tests/ui/mcp_config.example.json ~/.config/kilocode/mcp_config.json

# Или создайте вручную:
mkdir -p ~/.config/kilocode
cat > ~/.config/kilocode/mcp_config.json << 'EOF'
{
  "mcpServers": {
    "selenium": {
      "command": "npx",
      "args": ["-y", "@angiejones/mcp-selenium"],
      "env": {
        "CHROME_PATH": "/data/data/com.termux/files/usr/bin/chromium-browser",
        "CHROMEDRIVER_PATH": "/data/data/com.termux/files/usr/bin/chromedriver",
        "SELENIUM_HEADLESS": "true"
      }
    }
  }
}
EOF
```

### Шаг 4: Перезапуск Kilo Code

Перезапустите Kilo Code, чтобы он загрузил новую конфигурацию MCP.

## 🎯 Использование

После перезапуска Kilo Code, просто дайте ИИ задачу:

```
Протестируй главную страницу http://localhost:5000/manage
```

ИИ автоматически:
1. ✅ Запустит браузер
2. ✅ Перейдёт на страницу
3. ✅ Проверит элементы
4. ✅ Найдёт баги
5. ✅ Сделает скриншоты
6. ✅ Предоставит отчёт

## 📚 Дополнительная документация

- [Полная инструкция по MCP Selenium](./MCP_SELENIUM_SETUP.md) - детальное руководство
- [Инструкция для ИИ по Selenium](./AI_INSTRUCTION.md) - как ИИ должен писать тесты
- [Общая документация UI тестов](./README.md) - описание проекта

## 🐛 Решение проблем

### "Cannot find module @angiejones/mcp-selenium"
```bash
npm install -g @angiejones/mcp-selenium
```

### "ChromeDriver not found"
```bash
pkg reinstall chromium
```

### "MCP server not found"
1. Проверьте конфигурацию: `cat ~/.config/kilocode/mcp_config.json`
2. Перезапустите Kilo Code
3. Проверьте логи Kilo Code

## 💡 Примеры задач

### Проверка кнопки
```
Перейди на http://localhost:5000/manage, найди кнопку "Создать бота" и проверь, что она кликается
```

### Заполнение формы
```
На странице http://localhost:5000/manage найди форму создания бота, заполни поля:
- имя: Test Bot
- токен: test_token_123
и отправь форму
```

### Поиск багов
```
Проверь страницу http://localhost:5000/manage/flow-editor на наличие:
- отсутствующих элементов
- ошибок в консоли
- проблем с отображением
```

### Скриншот
```
Сделай скриншот страницы http://localhost:5000/manage после загрузки и сохрани в файл
```

## 🎉 Готово!

Теперь ИИ может автоматически тестировать ваш UI! Просто описывайте задачи на естественном языке.

---

**Поддержка:** Если что-то не работает, см. [полную инструкцию](./MCP_SELENIUM_SETUP.md)
