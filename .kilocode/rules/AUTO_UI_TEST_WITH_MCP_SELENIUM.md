# 🤖 Автоматическое UI тестирование через MCP Selenium

## Когда применять

При изменении файлов, влияющих на веб-интерфейс:
- `templates/*.html` - HTML шаблоны
- `static/**/*.css` - CSS стили
- `static/**/*.js` - JavaScript код
- `src/app.py` - Flask routes и обработчики

## Алгоритм

1. **Определи, что изменилось**
   - Какой файл изменён?
   - Какая функциональность затронута?

2. **Запусти Flask** (если не запущен)
   ```bash
   curl -s http://localhost:5000/manage > /dev/null 2>&1 || \
   (export APPLICATION_ROOT=/manage && python src/app.py > /dev/null 2>&1 &)
   ```

3. **Протестируй через MCP Selenium**
   - Запусти браузер: `mcp--selenium--start_browser`
   - Перейди на нужную страницу: `mcp--selenium--navigate`
   - Проверь изменённые элементы: `mcp--selenium--find_element`
   - Сделай скриншот: `mcp--selenium--take_screenshot`
   - Закрой браузер: `mcp--selenium--close_session`

4. **Предоставь отчёт**
   ```
   ✅ Работает или ❌ Проблемы
   Краткое описание проверки
   ```

## Примеры проверки

**Изменён HTML шаблон:** Проверь загрузку страницы и наличие новых элементов
**Изменён CSS:** Проверь отображение стилей
**Изменён JS:** Проверь работу интерактивных элементов
**Изменён Flask route:** Проверь переход по URL

---

**Правило применяется автоматически для UI изменений.**
