# Max Bot Platform - Платформа управления чат-ботами для участников СВО

## Описание проекта

Max Bot Platform - это веб-платформа для создания, управления и мониторинга множества чат-ботов на базе платформы Max.ru. Платформа предоставляет удобный веб-интерфейс для управления ботами, визуальный редактор потоков (Flow Editor) и инструменты для мониторинга работы ботов в реальном времени.

### Основные возможности

- **Управление множеством ботов**: создание, редактирование, запуск и остановка ботов через веб-интерфейс
- **Визуальный редактор потоков**: конструктор диалогов с drag-and-drop интерфейсом
- **Мониторинг в реальном времени**: отслеживание статуса ботов и просмотр логов
- **Гибкая конфигурация**: настройка токенов, базовых URL и других параметров для каждого бота
- **REST API**: программное управление ботами через HTTP API

## Функциональность ботов

Боты, созданные на платформе, предоставляют консультации по:

- **Консультации по социальной поддержке**: информация о всех доступных мерах поддержки
- **Льготы и выплаты**: подробности о льготах и выплатах для участников СВО и ветеранов
- **Медицинская помощь**: информация о медицинской, юридической и психологической помощи
- **Оформление документов**: помощь в порядке оформления документов для получения мер поддержки
- **Подача заявок**: возможность подать заявки на реабилитационный сертификат и социальное такси

## Структура проекта

```
new_max_bot/
├── src/                          # Исходный код приложения
│   ├── app.py                    # Flask приложение с REST API
│   ├── bot_manager.py            # Менеджер запущенных ботов
│   └── database.py               # Работа с SQLite базой данных
├── static/                       # Статические файлы веб-интерфейса
│   ├── app.js                    # Клиентская логика
│   ├── style.css                 # Стили
│   └── js/flow.js               # JavaScript для Flow Editor
├── templates/                    # HTML шаблоны
│   ├── index.html                # Главная страница
│   └── flow_editor.html          # Редактор потоков
├── flow_editor/                  # Модуль редактора потоков
├── tests/                        # Тесты
│   └── ui/                       # UI тесты (Selenium/Playwright)
├── docs/                         # Документация
│   └── README.md                 # Этот файл
├── data/                         # Данные приложения
│   └── logs/                     # Логи работы ботов
├── .env.example                  # Пример конфигурации
└── requirements.txt              # Python зависимости
```

## Установка и запуск

### Требования

- **Python 3.7+** - основной язык разработки
- **Flask 3.0+** - веб-фреймворк
- **SQLite** - база данных (входит в состав Python)
- **Node.js 18+** (опционально) - для UI тестов

### Установка зависимостей

**Python зависимости:**
```bash
pip install -r requirements.txt
```

**Node.js зависимости (для UI тестов, опционально):**
```bash
cd tests/ui
npm install --no-bin-links
cd ../..
```

### Настройка

1. Склонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd new_max_bot
   ```

2. Выполните установку зависимостей (см. выше)

3. Создайте файл `.env` из примера:
   ```bash
   cp .env.example .env
   ```

4. Отредактируйте файл `.env` при необходимости:
   ```env
   # Базовый путь для приложения
   # Используйте '/manage' если сайт доступен по адресу https://example.com/manage
   # Используйте '' (пустую строку) если сайт доступен по адресу https://example.com
    APPLICATION_ROOT=/manage
    ```

5. Запустите приложение:
    ```bash
    python src/app.py
    ```

6. Откройте браузер и перейдите по адресу:
   - `http://localhost:5000` (если `APPLICATION_ROOT` пустой)
   - `http://localhost:5000/manage` (если `APPLICATION_ROOT=/manage`)

### Создание первого бота

1. Откройте веб-интерфейс
2. Нажмите кнопку "Создать бота"
3. Заполните форму:
   - **Название бота**: например, "Информационный бот"
   - **Токен**: токен авторизации от Max Platform
   - **Base URL**: `https://platform-api.max.ru` (по умолчанию)
4. Нажмите "Создать"

### Запуск UI тестов (опционально)

```bash
cd tests/ui
npm test
```

Подробнее о тестировании см. в [`tests/ui/README.md`](../tests/ui/README.md)

## Веб-интерфейс

### Главная страница

Главная страница предоставляет список всех созданных ботов с их статусом:

- **Название** - имя бота
- **Статус** - текущий статус (запущен/остановлен)
- **Действия** - кнопки управления:
  - 🟢 **Запустить** - запустить бота
  - 🔴 **Остановить** - остановить бота
  - 🔄 **Перезапустить** - перезапустить бота
  - ✏️ **Редактировать** - изменить настройки
  - 🗑️ **Удалить** - удалить бота
  - 📋 **Логи** - просмотр логов работы бота
  - 🔧 **Flow Editor** - открыть редактор потоков

### Создание и редактирование ботов

При создании или редактировании бота вы можете настроить:

- **Название** - отображаемое имя бота
- **Токен** - токен авторизации Max Platform
- **Base URL** - базовый URL API (по умолчанию `https://platform-api.max.ru`)

### Flow Editor (Редактор потоков)

Flow Editor позволяет визуально создавать диалоговые сценарии:

- **Узлы меню** - создают пункты меню с кнопками
- **Узлы сообщений** - отправляют текстовые сообщения
- **Соединения** - связывают узлы для создания логики диалога

## REST API

Платформа предоставляет REST API для программного управления ботами:

### Эндпоинты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/bots` | Получить список всех ботов |
| POST | `/api/bots` | Создать нового бота |
| GET | `/api/bots/<id>` | Получить информацию о боте |
| PUT | `/api/bots/<id>` | Обновить информацию о боте |
| DELETE | `/api/bots/<id>` | Удалить бота |
| POST | `/api/bots/<id>/start` | Запустить бота |
| POST | `/api/bots/<id>/stop` | Остановить бота |
| POST | `/api/bots/<id>/restart` | Перезапустить бота |
| GET | `/api/bots/<id>/logs` | Получить логи бота |
| DELETE | `/api/bots/<id>/logs` | Очистить логи бота |
| GET | `/api/bots/<id>/flow` | Получить flow бота |
| PUT | `/api/bots/<id>/flow` | Обновить flow бота |

## Диагностика и устранение неполадок

### Логирование

Логи работы ботов хранятся в директории [`data/logs/`](../data/logs/). Вы можете:

- Просматривать логи через веб-интерфейс (кнопка "Логи")
- Очищать логи через веб-интерфейс
- Просматривать логи напрямую в файловой системе

### Настройка токена

Если бот не запускается или не отвечает, проверьте:

1. **Валидность токена** - проверьте токен через веб-интерфейс
2. **Права доступа** - убедитесь, что токен имеет необходимые права
3. **Base URL** - проверьте, что BASE_URL указан правильно

## API интеграция с Max Platform

Боты на платформе используют Max Platform API для взаимодействия с платформой MAX. Документация API доступна по адресу: https://dev.max.ru/docs-api

### Используемые методы API

- **Получение обновлений** (`GET /updates`) - Long polling для получения сообщений и колбэков
- **Отправка сообщений** (`POST /messages`) - Отправка текстовых сообщений и inline-клавиатур
- **Ответ на колбэки** (`POST /answers`) - Обработка нажатий на кнопки
- **Информация о боте** (`GET /bot`) - Получение информации о текущем боте

### Аутентификация

Используется заголовок `Authorization: <token>` для аутентификации запросов к API.

### Ограничения

- Максимальное количество запросов: 30 rps на platform-api.max.ru
- Поддерживается только протокол HTTPS для вебхуков

### Inline-клавиатура

Боты поддерживают inline-клавиатуры с кнопками типов:
- `callback` - для обработки нажатий на сервере
- `open_app` - для открытия мини-приложений (подача заявок)

Пример структуры клавиатуры:

```python
keyboard = {
    "type": "inline_keyboard",
    "payload": {
        "buttons": [
            [{"type": "callback", "text": "Кнопка 1", "payload": "action1"}],
            [{"type": "open_app", "text": "Открыть приложение", "payload": "app_id"}]
        ]
    }
}
```

## Категории поддержки

Боты, созданные на платформе, могут предоставлять информацию по следующим категориям:

### Ветераны боевых действий
- Выплаты и льготы
- Транспортные льготы
- Имущественная поддержка
- Льготное питание
- Медицинская помощь и надомный уход
- Обеспечение лекарствами
- Отдых и оздоровление
- Психологическая помощь
- Социальная адаптация и трудоустройство
- Технические средства реабилитации
- Юридическая помощь
- Льготное посещение спортивных объектов

### Участники СВО
- Выплаты и льготы
- Имущественная поддержка
- Медицинская помощь и надомный уход
- Отдых и оздоровление
- Психологическая помощь
- Социальная адаптация и трудоустройство
- Юридическая помощь
- Льготное питание

## Архитектура

### Компоненты системы

- **[`src/app.py`](../src/app.py)** - Flask приложение с REST API
  - Обрабатывает HTTP запросы от веб-интерфейса
  - Предоставляет API для управления ботами
  - Рендерит HTML шаблоны

- **[`src/bot_manager.py`](../src/bot_manager.py)** - Менеджер ботов
  - Управляет жизненным циклом ботов
  - Запускает и останавливает боты
  - Отслеживает статус ботов
  - Управляет потоками выполнения

- **[`src/database.py`](../src/database.py)** - Слой работы с данными
  - SQLite база данных для хранения настроек
  - CRUD операции для ботов
  - Управление flow (диалоговыми сценариями)
  - Работа с логами

### Основные функции

**BotManager:**
- `start_bot()` - запуск бота
- `stop_bot()` - остановка бота
- `restart_bot()` - перезапуск бота
- `get_bot_status()` - получение статуса бота
- `get_all_statuses()` - получение статусов всех ботов

**Database:**
- `add_bot()` - создание нового бота
- `get_bot()` - получение информации о боте
- `get_all_bots()` - получение списка всех ботов
- `update_bot()` - обновление настроек бота
- `delete_bot()` - удаление бота
- `save_bot_flow()` - сохранение flow бота
- `get_bot_flow()` - получение flow бота

**Bot Instance:**
- `get_updates()` - получение обновлений от API
- `send_message()` - отправка сообщений пользователям
- `answer_callback()` - обработка нажатий на кнопки
- `handle_message()` - обработка входящих сообщений
- `handle_callback()` - обработка колбэков от кнопок

### Flow Editor

Редактор потоков позволяет визуально создавать диалоговые сценарии:

**Типы узлов:**
- **menu** - узел меню с кнопками
- **message** - узел текстового сообщения

**Структура flow:**
```json
{
  "nodes": [
    {
      "id": "start",
      "type": "menu",
      "x": 100,
      "y": 100,
      "text": "Текст сообщения",
      "buttons": [
        {"id": "btn1", "text": "Кнопка 1"}
      ],
      "isStart": true
    }
  ],
  "connections": [
    {"from": "start", "to": "node2", "output": "btn1"}
  ]
}
```

## Безопасность

### Защита токенов

- **Токены хранятся в базе данных** - токены шифруются при хранении
- **Переменные окружения** - используйте `.env` файл для конфигурации (не коммитьте в git)
- **`.gitignore`** - файл `.env` исключен из git для защиты конфиденциальных данных

### Рекомендации по безопасности

1. **Не публикуйте токены** в открытых репозиториях
2. **Используйте разные токены** для разных окружений (dev/staging/prod)
3. **Регулярно обновляйте токены** по рекомендации Max Platform
4. **Ограничьте права доступа** токенов только необходимыми scope
5. **Используйте HTTPS** для production развертываний
6. **Настройте фаервол** для ограничения доступа к API

### Аутентификация в веб-интерфейсе

Текущая версия не имеет встроенной аутентификации. Рекомендуется:

- Использовать reverse proxy (nginx) с базовой аутентификацией
- Ограничить доступ по IP-адресам
- Добавить OAuth/OpenID Connect для enterprise-версий

## Развертывание

### Локальная разработка

```bash
python src/app.py
```

### Production развертывание

**Варианты развертывания:**
- **systemd service** - для Linux серверов
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация контейнеров
- **Supervisor** - управление процессами

### Настройка APPLICATION_ROOT

Параметр `APPLICATION_ROOT` в `.env` определяет базовый путь приложения:

```env
# Приложение доступно по корневому URL
APPLICATION_ROOT=

# Приложение доступно по /manage
APPLICATION_ROOT=/manage

# Приложение доступно по /bot
APPLICATION_ROOT=/bot
```

Это полезно при развертывании на shared хостинге или за reverse proxy.

## Тестирование

### UI тесты

```bash
cd tests/ui
npm test
```

Подробнее о тестировании см. в [`tests/ui/README.md`](../tests/ui/README.md)

## Мониторинг и логирование

### Логи

Логи работы ботов хранятся в [`data/logs/`](../data/logs/):

- Каждый бот имеет свой файл логов
- Логи доступны через веб-интерфейс
- Поддерживается очистка логов

### Мониторинг

Рекомендуется использовать:

- **Prometheus + Grafana** - для метрик
- **ELK Stack** - для агрегации логов
- **Sentry** - для отслеживания ошибок

## Дополнительная документация

- [`tests/ui/README.md`](../tests/ui/README.md) - Документация UI тестов

## Поддержка и развитие

### Получение помощи

- **Документация Max Platform**: https://dev.max.ru/docs-api
- **Issues**: создайте issue для баг-репортов
- **Pull Requests**: приветствуются вклады в разработку

### Roadmap

- [ ] Аутентификация в веб-интерфейсе
- [ ] Поддержка вебхуков
- [ ] Импорт/экспорт flow
- [ ] Шаблоны ботов
- [ ] Мультиязычность
- [ ] Аналитика и метрики
- [ ] Интеграция с внешними сервисами

## Лицензия

Укажите лицензию вашего проекта здесь.

---

**Max Bot Platform** - © 2024 Все права защищены.