@echo off
REM Скрипт для запуска приложения с правильной конфигурацией (Windows)

REM Устанавливаем переменную окружения для работы по пути /manage
set APPLICATION_ROOT=/manage

echo ================================================
echo 🚀 Запуск Max Bot Manager
echo ================================================
echo 📍 APPLICATION_ROOT=%APPLICATION_ROOT%
echo 🌐 Приложение будет доступно по адресам:
echo    http://localhost:5000%APPLICATION_ROOT%
echo    http://localhost:5000%APPLICATION_ROOT%/
echo ================================================
echo.

REM Проверяем существование .env файла
if not exist .env (
    echo ℹ️  Файл .env не найден (используем переменную окружения)
    echo 📝 Для создания .env файла выполните:
    echo    copy .env.example .env
    echo.
)

REM Проверяем, установлен ли python-dotenv
python -c "import dotenv" 2>nul
if errorlevel 1 (
    echo ⚠️  python-dotenv не установлен.
    echo 📦 Для использования .env файла выполните:
    echo    pip install python-dotenv
    echo.
)

REM Запускаем приложение
python src\app.py
